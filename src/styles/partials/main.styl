/**
 * Main styles
 */
*,
*:before,
*:after {
  box-sizing: border-box;
}

body {
  min-width: 320px;
}

/**
 * TODO: Move .spinner to separate file
 */
.spinner {
  position: fixed;
  z-index: 9999;
  top: 50%;
  left: 50%;

  display: block;
  overflow: hidden;

  text-align: center;
}